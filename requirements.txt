# ── CrewAI ────────────────────────────────────────────────────────────────────
# Pinned to 0.28.8 — this version has a stable API and is confirmed to work
# with LangChain 0.2+ and Groq. Newer versions (0.30+) changed the API
# significantly (removed Task.callback, changed Agent parameters).
# If you see import errors, verify this exact version is installed.
crewai==0.28.8

# ── LangChain ─────────────────────────────────────────────────────────────────
# Core framework used by both CrewAI (internally) and our RAG pipeline.
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# Required for RecursiveCharacterTextSplitter
# (moved out of langchain core in v0.2 — same fix as Project 5)
langchain-text-splitters>=0.2.0

# Groq LLM integration for LangChain (ChatGroq class)
langchain-groq>=0.1.0

# HuggingFace embeddings integration
langchain-huggingface>=0.0.3

# ── Vector Search ─────────────────────────────────────────────────────────────
# CPU version required — Streamlit Cloud has no GPU
faiss-cpu>=1.7.4

# ── Embeddings Model ──────────────────────────────────────────────────────────
# Downloads all-MiniLM-L6-v2 (~80 MB) from HuggingFace on first run
sentence-transformers>=2.2.0

# ── LLM Provider ──────────────────────────────────────────────────────────────
groq>=0.9.0

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.32.0
